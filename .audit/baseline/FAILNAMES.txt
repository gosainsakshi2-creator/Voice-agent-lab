B1 — a single "Hello?" mid-block gets one short acknowledgement, with NO language-model request
B2 — the acknowledgement is SHORT
B3 — a RESUME is progress and does not consume the cap
B4 — a repeated hello NEVER re-speaks the campaign opening
C1 — "Yes, I can hear you." resumes the block, without a language-model request
C2 — a bare "Haan" after the acknowledgement resumes too
C3 — a SECOND hello resumes rather than acknowledging again
C4 — the resumed text is in the history the model is next shown
D1 — "Hello? What is this about?" is answered contextually, not acknowledged
D2 — a real question RELEASES the held script position
G1 — a real interruption still barges in and is answered by the model
G2 — only the part of the block the caller HEARD is committed (unchanged)
H1 — no sentence the caller has heard is ever synthesized a second time
H2 — the resume is a strict suffix of the interrupted reply
I1 — the call carries on normally after an attention-check episode
J1 — "Hello? Hello?" over a playing block STOPS it at once, asks the fixed question, and listens
J2 — "Hello? Hello? Can you hear me?" over the block: cut, asked once, no generation
J3 — a single "Hello" over the block keeps the EXISTING decision: it already interrupts and is asked about once
J4 — "Yes." after the question RESUMES the block from where it stopped, without the model
J5 — "Yes, continue from where you stopped." RESUMES from the stopping point, not from the top
J6 — "Start from the beginning." REPEATS the whole cut-off reply from its first word, without the model
J7 — "No, I couldn't hear you." also REPEATS the cut-off reply from the beginning
J8 — a substantive answer after the question takes the NORMAL path, and the held position is released
a genuine interruption still interrupts, and is still answered
